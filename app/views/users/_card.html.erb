<div class="col-xs-6 col-sm-2 col-md-6">
  <div class="card-team">
    <div class="card-body text-center">
      <%= image_tag "avatar_pics/#{user.first_name}.jpg", class: "img-fluid" %>
      <h4 class="card-title"><%= user.first_name %> <%= user.last_name %></h4>
      <ul class="nav nav-tabs" id="myTab" role="tablist">
        <li class="nav-item">
          <a class="nav-link active" id="hours-tab" data-toggle="tab" href="#hours-<%= user.id %>" role="tab" aria-controls="hours" aria-selected="true">Hours</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" id="budget-tab" data-toggle="tab" href="#budget-<%= user.id %>" role="tab" aria-controls="budget" aria-selected="false">Budget</a>
        </li>
      </ul>
      <div class="tab-content" id="myTabContent">
        <div class="tab-pane fade show active" id="hours-<%= user.id %>" role="tabpanel" aria-labelledby="home-tab">
          <canvas class='user-chart' width="400" height="400" data-projects-hours="<%= user.total_hours_spent %>"></canvas>
        </div>
        <div class="tab-pane fade" id="budget-<%= user.id %>" role="tabpanel" aria-labelledby="profile-tab">
         <canvas class='user-chart' width="400" height="400" data-projects-budgets="<%= user.total_budgets_spent %>"></canvas>
        </div>
      </div>
    </div>
  </div>
</div>
