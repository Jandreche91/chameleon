<div class="container">
  <h1>Edit the project</h1>
  <%= simple_form_for(@project) do |f| %>
    <%= f.input :name %>
    <%= f.input :description, as: :text %>
    <%= f.input :estimated_cost %>
    <%= f.input :estimated_start_date %>
    <%= f.input :estimated_end_date %>
<!--     <#%= f.association :user, collection: User.all, as: :check_boxes %>
    <#%= f.association :assignments, collection: Assignment.all, as: :check_boxes %> -->
    <%#= f.association :user, collection: User.all.map{|user| [user.name, user.id] }, as: :check_boxes %>
    <%= f.button :submit, class: "btn-primary" %>
  <% end %>
</div>
